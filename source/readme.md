now using log file for parsing keryx

added: no-docker_launcher.sh -- same miner conf,api,etc no docker

keryx-dummy-cpu-service.sh -- keryx-miner service named same as cpu service for easy dashboard control
